spring:
  datasource:
#    url: jdbc:postgresql://localhost:5432/postgresql
    # 인스턴스 내부 ip
    url: jdbc:postgresql://10.140.0.4/postgresql
    username: postgresql
    password: postgrespassword
  jpa:
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: update